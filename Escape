local Players = game:GetService("Players")
local Lighting = game:GetService("Lighting")
local LocalPlayer = Players.LocalPlayer
local BasesFolder = workspace:FindFirstChild("Bases")

local LOAD_WAIT = 4
print("⏳ Wachten op laden... (" .. LOAD_WAIT .. "s)")
task.wait(LOAD_WAIT)

BasesFolder = workspace:WaitForChild("Bases", 20)

local char = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
local root = char:WaitForChild("HumanoidRootPart")

LocalPlayer.CharacterAdded:Connect(function(newChar)
    char = newChar
    root = newChar:WaitForChild("HumanoidRootPart")
end)

-- Plot vinden
local myPlot = nil
if BasesFolder and root then
    local minDistance = math.huge
    for _, plot in ipairs(BasesFolder:GetChildren()) do
        local ref = plot:FindFirstChildWhichIsA("BasePart", true)
        if ref then
            local dist = (ref.Position - root.Position).Magnitude
            if dist < minDistance then
                minDistance = dist
                myPlot = plot
            end
        end
    end
end

print(myPlot and "✅ Plot: " .. myPlot.Name or "⚠️ Geen plot gevonden!")

local PROTECTED_KEYWORDS = {"Craft", "Station", "Portal", "Upgrade", "Shop", "Spawn", "Base"}
local function isProtected(obj)
    local name = obj.Name:lower()
    for _, word in ipairs(PROTECTED_KEYWORDS) do
        if name:find(word:lower()) then return true end
    end
    return false
end

-- Lighting optimaliseren
pcall(function()
    Lighting.Technology = Enum.Technology.Compatibility
    Lighting.GlobalShadows = false
    Lighting.Brightness = 2
    Lighting.ClockTime = 14
end)

for _, e in ipairs(Lighting:GetChildren()) do
    pcall(function()
        if e:IsA("PostEffect") then e.Enabled = false end
    end)
end

-- Terrain platleggen
pcall(function()
    workspace.Terrain.WaterWaveSize = 0
    workspace.Terrain.WaterWaveSpeed = 0
    workspace.Terrain.Decoration = false
end)

-- ==========================================
-- JOUW EIGEN BASE (Veilig Strippen)
-- ==========================================
if myPlot then
    for _, obj in ipairs(myPlot:GetDescendants()) do
        pcall(function()
            if obj:IsA("BasePart") then
                obj.CastShadow = false
                -- GEEN Destroy() meer hier! Jouw blokjes blijven netjes staan.
            end
        end)
    end
end

-- ==========================================
-- ANDERE BASES (Volledig Onzichtbaar / Geen Collision)
-- ==========================================
if BasesFolder then
    for _, plot in ipairs(BasesFolder:GetChildren()) do
        if plot ~= myPlot then
            plot.DescendantAdded:Connect(function(desc)
                pcall(function()
                    if desc:IsA("BasePart") then
                        desc.Transparency = 1
                        desc.CanCollide = false
                        desc.CastShadow = false
                    elseif desc:IsA("Decal") or desc:IsA("Texture") or desc:IsA("ParticleEmitter") then
                        desc.Transparency = 1
                    end
                end)
            end)
            for _, obj in ipairs(plot:GetDescendants()) do
                pcall(function()
                    if obj:IsA("BasePart") then
                        obj.Transparency = 1
                        obj.CanCollide = false
                        obj.CastShadow = false
                    elseif obj:IsA("Decal") or obj:IsA("Texture") then
                        obj.Transparency = 1
                    elseif obj:IsA("ParticleEmitter") or obj:IsA("Beam") or obj:IsA("Light") then
                        obj.Enabled = false
                    elseif obj:IsA("Sound") then
                        obj.Volume = 0
                    end
                end)
            end
        end
    end
end

-- ==========================================
-- WORKSPACE (Veilig Onzichtbaar Maken)
-- ==========================================
for _, obj in ipairs(workspace:GetChildren()) do
    if obj == char or obj == BasesFolder or obj:IsA("Terrain") or obj:IsA("Camera") or isProtected(obj) then continue end
    
    pcall(function()
        -- In plaats van de lobby te slopen, maken we hem onzichtbaar met collision AAN.
        -- Zo val je nooit meer door de map heen bij spawn.
        if obj:IsA("BasePart") then
            obj.Transparency = 1
            obj.CastShadow = false
        end
        for _, desc in ipairs(obj:GetDescendants()) do
            if desc:IsA("BasePart") then
                desc.Transparency = 1
                desc.CastShadow = false
            end
        end
    end)
end

-- Animaties uitzetten
for _, player in ipairs(Players:GetPlayers()) do
    if player ~= LocalPlayer and player.Character then
        for _, obj in ipairs(player.Character:GetDescendants()) do
            pcall(function()
                if obj:IsA("Animator") then
                    for _, track in ipairs(obj:GetPlayingAnimationTracks()) do track:Stop() end
                end
            end)
        end
    end
end

Players.PlayerAdded:Connect(function(player)
    player.CharacterAdded:Connect(function(otherChar)
        task.wait(1)
        for _, obj in ipairs(otherChar:GetDescendants()) do
            pcall(function()
                if obj:IsA("Animator") then
                    for _, track in ipairs(obj:GetPlayingAnimationTracks()) do track:Stop() end
                end
            end)
        end
    end)
end)

-- Live strippen (Zonder map te slopen)
workspace.ChildAdded:Connect(function(obj)
    if obj == char or obj == BasesFolder or obj:IsA("Terrain") or obj:IsA("Camera") or isProtected(obj) then return end
    task.defer(function()
        pcall(function() 
            if obj:IsA("BasePart") then
                obj.Transparency = 1
                obj.CastShadow = false
            end
            for _, desc in ipairs(obj:GetDescendants()) do
                if desc:IsA("BasePart") then
                    desc.Transparency = 1
                    desc.CastShadow = false
                end
            end
        end)
    end)
end)

print("✅ Perfect gestript! Je valt niet meer door de map.")
