-- ==========================================
-- Jouw eigen base VISUEEL strippen (Niet slopen!)
-- ==========================================
if myPlot then
    for _, obj in ipairs(myPlot:GetDescendants()) do
        pcall(function()
            if obj:IsA("BasePart") then
                obj.CastShadow = false
                -- We gebruiken GEEN Destroy(). Zo behoud je je eigen base intact, 
                -- maar het kost je GPU geen moeite meer om schaduwen te renderen.
            end
        end)
    end
end

-- ==========================================
-- Andere bases onzichtbaar maken (Geen Collision)
-- ==========================================
if BasesFolder then
    for _, plot in ipairs(BasesFolder:GetChildren()) do
        if plot ~= myPlot then
            plot.DescendantAdded:Connect(function(desc)
                pcall(function()
                    if desc:IsA("BasePart") then
                        desc.Transparency = 1
                        desc.CanCollide = false -- Mag hier wel uit, want je bent niet in andermans base
                        desc.CastShadow = false
                    elseif desc:IsA("Decal") or desc:IsA("Texture") then
                        desc.Transparency = 1
                    elseif desc:IsA("ParticleEmitter") or desc:IsA("Beam")
                        or desc:IsA("Light") or desc:IsA("Trail")
                        or desc:IsA("Fire") or desc:IsA("Smoke")
                        or desc:IsA("Sparkles") then
                        desc.Enabled = false
                    end
                end)
            end)
            for _, obj in ipairs(plot:GetDescendants()) do
                pcall(function()
                    if obj:IsA("BasePart") then
                        obj.Transparency = 1
                        obj.CanCollide = false
                        obj.CastShadow = false
                    elseif obj:IsA("Decal") or obj:IsA("Texture") then
                        obj.Transparency = 1
                    elseif obj:IsA("ParticleEmitter") or obj:IsA("Beam")
                        or obj:IsA("Light") or obj:IsA("Trail")
                        or obj:IsA("Fire") or obj:IsA("Smoke")
                        or obj:IsA("Sparkles") then
                        obj.Enabled = false
                    elseif obj:IsA("Sound") then
                        obj.Volume = 0
                    end
                end)
            end
        end
    end
end

-- ==========================================
-- Workspace strippen buiten Bases (VEILIG)
-- ==========================================
for _, obj in ipairs(workspace:GetChildren()) do
    if obj == char then continue end
    if obj == BasesFolder then continue end
    if obj:IsA("Terrain") then continue end
    if obj:IsA("Camera") then continue end
    if isProtected(obj) then continue end
    
    pcall(function()
        -- In plaats van de lobby/map te Destroyen, maken we hem onzichtbaar.
        -- We laten collision AAN, zodat je niet door de map valt.
        if obj:IsA("BasePart") then
            obj.Transparency = 1
            obj.CastShadow = false
        end
        for _, desc in ipairs(obj:GetDescendants()) do
            if desc:IsA("BasePart") then
                desc.Transparency = 1
                desc.CastShadow = false
                -- desc.CanCollide blijft intact!
            elseif desc:IsA("Decal") or desc:IsA("Texture") then
                desc.Transparency = 1
            elseif desc:IsA("ParticleEmitter") or desc:IsA("Beam") or desc:IsA("Light") then
                desc.Enabled = false
            end
        end
    end)
end

-- ==========================================
-- Animaties andere spelers uitzetten
-- ==========================================
for _, player in ipairs(Players:GetPlayers()) do
    if player ~= LocalPlayer then
        local otherChar = player.Character
        if otherChar then
            for _, obj in ipairs(otherChar:GetDescendants()) do
                pcall(function()
                    if obj:IsA("Animator") then
                        for _, track in ipairs(obj:GetPlayingAnimationTracks()) do
                            track:Stop()
                        end
                    end
                end)
            end
        end
    end
end

Players.PlayerAdded:Connect(function(player)
    player.CharacterAdded:Connect(function(otherChar)
        task.wait(1)
        for _, obj in ipairs(otherChar:GetDescendants()) do
            pcall(function()
                if obj:IsA("Animator") then
                    for _, track in ipairs(obj:GetPlayingAnimationTracks()) do
                        track:Stop()
                    end
                end
            end)
        end
    end)
end)

-- ==========================================
-- Live strippen (Minder agressief)
-- ==========================================
workspace.ChildAdded:Connect(function(obj)
    if obj == char then return end
    if obj == BasesFolder then return end
    if obj:IsA("Terrain") then return end
    if obj:IsA("Camera") then return end
    if isProtected(obj) then return end
    
    task.defer(function()
        pcall(function() 
            -- Nieuwe objecten maken we visueel leeg, maar we slopen ze niet.
            if obj:IsA("BasePart") then
                obj.Transparency = 1
                obj.CastShadow = false
            end
            for _, desc in ipairs(obj:GetDescendants()) do
                if desc:IsA("BasePart") then
                    desc.Transparency = 1
                    desc.CastShadow = false
                end
            end
        end)
    end)
end)

print("✅ Gestript zonder in de void te vallen!")
