-- ⚡ MzD ETFB RNG SCRIPT V4 (SELF-VERIFYING OVERLAY) ⚡
local RunService = game:GetService("RunService")
local Lighting = game:GetService("Lighting")
local Players = game:GetService("Players")
local GuiService = game:GetService("GuiService")
local StarterGui = game:GetService("StarterGui")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local LocalPlayer = Players.LocalPlayer

-- RENDERING UIT (Resource Saving)
pcall(function() RunService:Set3dRenderingEnabled(false) end)
Lighting.Brightness = 0
Lighting.ClockTime = 0
Lighting.FogEnd = 0
Lighting.GlobalShadows = false
Lighting.OutdoorAmbient = Color3.new(0,0,0)
Lighting.Ambient = Color3.new(0,0,0)
for _, effect in ipairs(Lighting:GetChildren()) do
    if effect:IsA("PostEffect") then effect.Enabled = false end
end
pcall(function() settings().Rendering.QualityLevel = Enum.QualityLevel.Level01 end)
StarterGui:SetCoreGuiEnabled(Enum.CoreGuiType.All, false)
pcall(function() GuiService:SetMenuEnabled(false) end)

-- Workspace Stripper
task.spawn(function()
    task.wait(2)
    local char = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
    for _, obj in ipairs(workspace:GetDescendants()) do
        if obj:IsDescendantOf(char) or obj:IsA("Terrain") then continue end
        pcall(function()
            if obj:IsA("BasePart") then
                obj.Transparency = 1
                obj.CastShadow = false
            elseif obj:IsA("Decal") or obj:IsA("Texture") then
                obj.Transparency = 1
            elseif obj:IsA("ParticleEmitter") or obj:IsA("Beam") or obj:IsA("Trail") then
                obj.Enabled = false
            elseif obj:IsA("Sound") then
                obj.Volume = 0
                obj:Stop()
            end
        end)
    end
end)

-- BLACK OVERLAY GUI
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "MzD_BlackOverlay"
ScreenGui.ResetOnSpawn = false
ScreenGui.IgnoreGuiInset = true
ScreenGui.DisplayOrder = 999
ScreenGui.Parent = LocalPlayer:WaitForChild("PlayerGui")

local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(1, 0, 1, 0)
MainFrame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
MainFrame.BorderSizePixel = 8
MainFrame.BorderColor3 = Color3.fromRGB(200, 0, 0)
MainFrame.Parent = ScreenGui

local ContentFrame = Instance.new("Frame")
ContentFrame.Size = UDim2.new(0.8, 0, 0.7, 0)
ContentFrame.Position = UDim2.new(0.1, 0, 0.15, 0)
ContentFrame.BackgroundTransparency = 1
ContentFrame.Parent = MainFrame

local TitleLabel = Instance.new("TextLabel")
TitleLabel.Size = UDim2.new(1, 0, 0.12, 0)
TitleLabel.Position = UDim2.new(0, 0, 0, 0)
TitleLabel.BackgroundTransparency = 1
TitleLabel.Text = "🎲 MzD ETFB RNG SCRIPT V4 🎲"
TitleLabel.TextColor3 = Color3.fromRGB(255, 0, 0)
TitleLabel.Font = Enum.Font.GothamBold
TitleLabel.TextScaled = true
TitleLabel.Parent = ContentFrame

-- 🟢 NIEUW: Knipperende Systeem-Hartslag (Laat zien dat script NIET bevroren is)
local HeartbeatLabel = Instance.new("TextLabel")
HeartbeatLabel.Size = UDim2.new(1, 0, 0.08, 0)
HeartbeatLabel.Position = UDim2.new(0, 0, 0.15, 0)
HeartbeatLabel.BackgroundTransparency = 1
HeartbeatLabel.Text = "⚡ SCRIPT STATUS: APPLICATIE DRAAIT [●]"
HeartbeatLabel.TextColor3 = Color3.fromRGB(0, 255, 0)
HeartbeatLabel.Font = Enum.Font.Code
HeartbeatLabel.TextScaled = true
HeartbeatLabel.Parent = ContentFrame

-- ⏱️ NIEUW: Onafhankelijke Uptime klok
local UptimeLabel = Instance.new("TextLabel")
UptimeLabel.Size = UDim2.new(1, 0, 0.08, 0)
UptimeLabel.Position = UDim2.new(0, 0, 0.25, 0)
UptimeLabel.BackgroundTransparency = 1
UptimeLabel.Text = "⏱️ Script Verstreken Tijd: 0s"
UptimeLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
UptimeLabel.Font = Enum.Font.Code
UptimeLabel.TextScaled = true
UptimeLabel.Parent = ContentFrame

-- 🛠️ NIEUW: Live Achtergrond Actie Log
local ActionLabel = Instance.new("TextLabel")
ActionLabel.Size = UDim2.new(1, 0, 0.08, 0)
ActionLabel.Position = UDim2.new(0, 0, 0.38, 0)
ActionLabel.BackgroundTransparency = 1
ActionLabel.Text = "🔄 Actie: Initialiseren..."
ActionLabel.TextColor3 = Color3.fromRGB(255, 255, 0)
ActionLabel.Font = Enum.Font.Gotham
ActionLabel.TextScaled = true
ActionLabel.Parent = ContentFrame

local InfinityLabel = Instance.new("TextLabel")
InfinityLabel.Size = UDim2.new(1, 0, 0.10, 0)
InfinityLabel.Position = UDim2.new(0, 0, 0.55, 0)
InfinityLabel.BackgroundTransparency = 1
InfinityLabel.Text = "0 number of Infinity counts?"
InfinityLabel.TextColor3 = Color3.fromRGB(170, 0, 255)
InfinityLabel.Font = Enum.Font.Gotham
InfinityLabel.TextScaled = true
InfinityLabel.Parent = ContentFrame

local EternalLabel = Instance.new("TextLabel")
EternalLabel.Size = UDim2.new(1, 0, 0.10, 0)
EternalLabel.Position = UDim2.new(0, 0, 0.70, 0)
EternalLabel.BackgroundTransparency = 1
EternalLabel.Text = "0 number of Eternal counts?"
EternalLabel.TextColor3 = Color3.fromRGB(255, 0, 0)
EternalLabel.Font = Enum.Font.Gotham
EternalLabel.TextScaled = true
EternalLabel.Parent = ContentFrame

-- ==========================================================================
-- LIVE ENGINE VOOR HIERBOVEN (Draait volledig los van de game)
-- ==========================================================================
task.spawn(function()
    local startTijd = tick()
    local switch = false
    while true do
        local totaalSeconds = math.floor(tick() - startTijd)
        local minuten = math.floor(totaalSeconds / 60)
        local seconden = totaalSeconds % 60
        UptimeLabel.Text = string.format("⏱️ Script Verstreken Tijd: %02dm %02ds", minuten, seconden)
        
        switch = not switch
        if switch then
            HeartbeatLabel.Text = "⚡ SCRIPT STATUS: APPLICATIE DRAAIT [●]"
            HeartbeatLabel.TextColor3 = Color3.fromRGB(0, 255, 0)
        else
            HeartbeatLabel.Text = "⚡ SCRIPT STATUS: APPLICATIE DRAAIT [○]"
            HeartbeatLabel.TextColor3 = Color3.fromRGB(0, 150, 0)
        end
        task.wait(1)
    end
end)

-- ITEM TRACKER (Backpack)
local infinityCount = 0
local eternalCount = 0
local trackedItems = {}
local backpack = LocalPlayer:WaitForChild("Backpack", 15)

if backpack then
    for _, item in ipairs(backpack:GetChildren()) do
        if not trackedItems[item] then
            trackedItems[item] = true
            local itemName = item.Name:lower()
            if string.find(itemName, "infinity") then infinityCount = infinityCount + 1
            elseif string.find(itemName, "eternal") then eternalCount = eternalCount + 1 end
        end
    end
    InfinityLabel.Text = infinityCount .. " number of Infinity counts?"
    EternalLabel.Text = eternalCount .. " number of Eternal counts?"
    
    backpack.ChildAdded:Connect(function(item)
        if trackedItems[item] then return end
        trackedItems[item] = true
        local itemName = item.Name:lower()
        if string.find(itemName, "infinity") then
            infinityCount = infinityCount + 1
            InfinityLabel.Text = infinityCount .. " number of Infinity counts?"
        elseif string.find(itemName, "eternal") then
            eternalCount = eternalCount + 1
            EternalLabel.Text = eternalCount .. " number of Eternal counts?"
        end
    end)
    backpack.ChildRemoved:Connect(function(item) trackedItems[item] = nil end)
end

-- AUTO UPGRADE (Met Live Visuele Status Updates)
local upgradeActive = true
local RF = ReplicatedStorage:WaitForChild("Shared"):WaitForChild("Remotes"):WaitForChild("Networking"):WaitForChild("RF/RNGUpgradePurchase")

local function safePurchase(name)
    local s, r = pcall(function() return RF:InvokeServer(name) end)
    if not s then return false end
    return r ~= false and r ~= nil
end

local function maxOutUpgrade(name)
    ActionLabel.Text = "⚙️ Upgraden: Bezig met " .. name .. "..."
    ActionLabel.TextColor3 = Color3.fromRGB(255, 180, 0)
    while upgradeActive do
        if not safePurchase(name) then break end
        task.wait(0.4)
    end
end

task.spawn(function()
    task.wait(3)
    while upgradeActive do
        maxOutUpgrade("RollSpeed")
        maxOutUpgrade("Luck")
        
        ActionLabel.Text = "⚙️ Upgraden: Bezig met Special Luck..."
        local inf_done, ete_done = false, false
        while not (inf_done and ete_done) and upgradeActive do
            if not inf_done then
                if not safePurchase("InfinityLuck") then inf_done = true else task.wait(0.4) end
            end
            if not ete_done then
                if not safePurchase("EternalLuck") then ete_done = true else task.wait(0.4) end
            end
        end
        
        maxOutUpgrade("TraitLuck")
        
        -- Rustperiode status
        ActionLabel.Text = "💤 Status: Upgrades voltooid. Wachten op geld..."
        ActionLabel.TextColor3 = Color3.fromRGB(0, 200, 255)
        task.wait(1800) -- 30 minuten rust
    end
end)

game:BindToClose(function() upgradeActive = false end)
print("✅ MzD SELF-VERIFYING SCRIPT GESTART")
